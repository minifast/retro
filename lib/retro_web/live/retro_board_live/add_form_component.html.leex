<%= f = form_for @changeset, "#",
  id: "add-retro-board-form",
  phx_target: @myself,
  phx_submit: "save_retro_board" %>

  <%= label f, :name %>
  <%= text_input f, :name %>
  <%= error_tag f, :name %>

  <%= submit "Add", phx_disable_with: "Adding..." %>
</form>
