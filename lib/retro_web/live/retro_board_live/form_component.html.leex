<h2><%= @title %></h2>

<%= f = form_for @changeset, "#",
  id: "retro-board-form",
  phx_target: @myself,
  phx_change: "validate",
  phx_submit: "save_retro_board" %>

  <%= label f, :name %>
  <%= text_input f, :name %>
  <%= error_tag f, :name %>

  <%= submit "Save", phx_disable_with: "Saving..." %>
</form>
